复用类 即 resuing class

1、初始化导出类时，会先初始化基类

-- 代理
Delegation，
代理 与 组合 我总是分不太清楚

相同的地方：
代理也是 将一个基类放在 代理的内部，
而组合 也是将一个基类放在 代理的内部。

不同的地方：
组合是为了扩展基类的方法吗？
代理则不是为了扩展基类的方法。

-- 结合使用组合和继承

-- 在组合（has-a）与继承（is-a）之间选择

组合和继承都允许新的类中放置子类对象，组合是显式这样做（代理貌似也是），继承是隐式这样做

is-a的关系

has-a的关系

-- final和private的关键字

private 方法都隐式地指定为final

如果视图覆盖一个private 的方法，似乎可以

